
  # RoverNote Travel Scrapbook

  This is a code bundle for RoverNote Travel Scrapbook. The original project is available at https://www.figma.com/design/ExpD6mpJva3eyhT1wXRfVb/RoverNote-Travel-Scrapbook.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  